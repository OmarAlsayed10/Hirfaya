import { COLORS } from "../../../theme/tokens";

export const careerRoadmapTokens = {
  primary: COLORS.primary,
  ink: COLORS.textPrimary,
  muted: COLORS.textSecondary,
  sand: COLORS.bgLight,
  amber: COLORS.accentOrange,
  border: COLORS.borderLight,
} as const;
