const livePreviewPane = {
  root: {
    width: '100%',
    height: '100%',
    overflow: 'hidden',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'flex-start',
  },
} as const;

export default livePreviewPane;
